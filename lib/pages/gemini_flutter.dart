import 'package:flutter/material.dart';

class GeminiFlutter extends StatelessWidget {
  const GeminiFlutter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gemini API Integration With Flutter'),
      ),
    );
  }
}
